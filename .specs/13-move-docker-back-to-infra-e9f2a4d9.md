# 13-move-docker-back-to-infra-e9f2a4d9.md

## Objective
Move `docker-compose.yml` back to `infra/` folder and update `scripts/run.sh` to reference it correctly.

## DoD
- [DONE] Move `docker-compose.yml` back to `infra/`.
- [DONE] Update `scripts/run.sh` to include `-f infra/docker-compose.yml`.
- [DONE] Verify tasks marked as done.
- [DONE] Status section updated to [DONE].

## Status
- [DONE] Move `docker-compose.yml` back to `infra/`.
- [DONE] Update `scripts/run.sh`.
