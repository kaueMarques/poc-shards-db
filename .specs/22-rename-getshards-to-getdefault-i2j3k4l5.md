# 22-rename-getshards-to-getdefault-i2j3k4l5.md

## Goal
Rename the `getShards()` method in `ShardRouter` to `getDefault()` to better reflect its purpose as the default routing behavior.

## Status
- [DONE] Rename method in `ShardRouter.java`.
- [DONE] Update `ShardRestController.java` references.

## Definition of Done (DoD)
- [ ] Method renamed to `getDefault()`.
- [ ] Controller updated to call `getDefault()`.
- [ ] Code properly structured.
- [ ] Project compiles.
- [ ] Documentation (`CHANGELOG.md`) updated.
