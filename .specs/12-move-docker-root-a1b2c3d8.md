# 12-move-docker-root-a1b2c3d8.md

## Objective
Move `docker-compose.yml` from `infra/` to the project root and update `scripts/run.sh` to reflect this change.

## DoD
- [DONE] Move `infra/docker-compose.yml` to the root directory.
- [DONE] Update `scripts/run.sh` to remove the `-f infra/docker-compose.yml` flag.
- [DONE] Verify the environment still starts correctly (mental check).
- [DONE] Status section updated to [DONE].
- [DONE] Tasks marked as done in todo.

## Status
- [DONE] Move `infra/docker-compose.yml` to root.
- [DONE] Update `scripts/run.sh`.
